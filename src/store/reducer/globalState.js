



const initialState = {
    userInfo:"",
    cargando:false,
    errorMensaje:""
}

const globalState = (state=initialState,action)=>{
    switch (action) {
        
        default:
            return state
    }
}

export default globalState;